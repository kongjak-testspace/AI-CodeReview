default:
  cli: claude
  language: en
  timeout: 600
  max_budget_usd: 1.0

repos:
  # Example: per-repo configuration overrides
  # "owner/repo-name":
  #   cli: codex
  #   language: ko
  #   timeout: 300
