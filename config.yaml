default:
  cli: claude
  fallback_cli:
    - codex
    - gemini
    - copilot
  review_mode: multi
  synthesizer_cli: claude
  language: en
  timeout: 600
  max_budget_usd: 1.0

repos:
  # Example: per-repo configuration overrides
  # "owner/repo-name":
  #   cli: codex
  #   fallback_cli: [claude, gemini]
  #   review_mode: single
  #   language: ko
  #   timeout: 300
